<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeekPlanner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow_200</item>
        <item name="colorSecondaryVariant">@color/yellow_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorControlActivated">@color/blue_500</item>
        <item name="colorControlHighlight">@color/blue_500</item>
    </style>

    <style name="Theme.WeekPlanner.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.WeekPlanner.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.WeekPlanner.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Text Style -->
    <style name="subtitleText">
        <item name="android:textSize">@dimen/subtitle_text_size</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <style name="bodyText">
        <item name="android:textSize">@dimen/body_text_size</item>
        <item name="android:letterSpacing">0.03125</item>
    </style>

</resources>